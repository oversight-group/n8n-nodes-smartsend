# Smart Send integration token (the LONG token, not the short workspace code)
SMARTSEND_ORG_ID=
SMARTSEND_BASE=https://smartsend-server.otherwise.co.il
# Only used by the opt-in live send test
SMARTSEND_TEST_PHONE=
